{
  "compilerOptions": {
    "target": "ES6",
    "module": "commonjs",
    "allowSyntheticDefaultImports": true,
    "baseUrl": "src",
    "jsx": "react",
    "checkJs": false // set to true to enable adding missing imports (accessed via
    // right click -> Source Action... -> Add all missing imports)
  }
}
